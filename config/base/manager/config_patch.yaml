apiVersion: apps/v1
kind: Deployment
metadata:
  name: skytorch-operator
spec:
  template:
    spec:
      containers:
      - name: manager
        args:
        - --config=/controller_manager_config.yaml
        - --zap-log-level=2
        volumeMounts:
        - name: skytorch-operator-config
          mountPath: /controller_manager_config.yaml
          subPath: config.yaml
          readOnly: true
      volumes:
      - name: skytorch-operator-config
        configMap:
          name: skytorch-operator-config
